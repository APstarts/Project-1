# How to run the project?
- First insall all the dependencies
    ```
        npm install
        or
        bun install
    ```
- Then run the below command to run and open in your browser:
    ```
    bun run dev
    or 
    npm run dev
    ```